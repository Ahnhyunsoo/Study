WCHAR = 2byte 문자열 자료형인 wchar_t를 typedfe로 이름재정의 해놓은 것이다.
WCHAR = 문자 배열문법이다.
SAL = 주석언어 
hInstance = 실행 된 프로세스의 시작 주소
hprevInstance = 이전에 실행된 프로세스의 주소 (과거엔 윈도우가 이렇게 작동했다. 하지만 현재는 몇개를 올리든 hInstance는 같은 값이 나온다)
같은 주소가 나오는 이유는 윈도우는 가상메모리를 사용하기 때문이다. 사용자로 하여금 윈도우는
각프로그램마다 고유한 자신의 영역이 있다고 생각하게 만든다.
_In_ = 주석언어 SAL 키워드로하여금 어떤 특징인지 알게끔하는 주석
_In opt = 의미가없는것
switch문에서 변수를 선언하고싶으면 케이스뒤에 중괄호를 써주면 된다.
curnel object OS에서 관리해주는 객체
커널 오브젝트는 우리한테 ID를 준다. 
ID값을 핸들이라고 하고 핸들을 이용해서 작업을 한다.