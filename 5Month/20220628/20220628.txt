IDirectDevice9** = LPDirectDevice*    LP가 롱포인터이기때문에 둘다 이중포인터라 똑같다.

이니셜라이저에 인자를넣어서 부모함수를 상속받는게 아닌 재정의한 함수

컴파일러가 구동코드를 생성하고 이 구동코드가 메인함수를 실행한다.

실제의 스레드가 리소스를 읽는 작업을 한다.

다른하나는 화면을 갱신해준다.

로더라는 클래스를 추가해서 인클루드 Base하고 네임스페이스를 클라이언트로 해준다.

private로 핸들을 포함한다. m_hThread;

쓰레드를 사용하려면 프로세스 헤더를 포함해줘여한다.

보이드포인터는 어떤타입이든 받을수있지만 사용하기전에 캐스팅을 해줘야한다.

3d에서는 깊이버퍼를 이용하기 때문에 객체그리는순서를 관리해주는 랜더라는 클래스를 작성할거여서 레벨의 랜더중요도가 떨어진다.