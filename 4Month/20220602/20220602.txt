c++ 추가포함디렉터리
../Header
싱글톤을 위한 복사생성자조차 못만들게 하기위해 사용하는 라이브러리 = NOCOPY
장치 = 랜더링 담당 객체
다이렉트x가 컴포넌트 객체를 만들 수 있다.
컴포넌트 객체 = 하드웨어에게 직접적인 명령을 내리는 객체

장치 초기화 과정
1. 랜더링 장치를 생성하고 조사할 객체 생성
2. 장치 조사 (지원 수준을 파악)
3. 랜더링 장치 제어 객체 생성

다이렉트 관련된 함수들은 흐름은 알아야하지만 달달달 외울 필요는 없다.

담당 객체들을 만들고 객체들을 통합하여 하드웨어를 제어한다.
다이렉트는 그래픽스엔진이다.
HRESULT = 초기화가 성공했을 때 0~ 양수를 리턴하고
초기화가 실패했을 때 음수를 반환한다.
버텍스 프로세싱 = 정점의변환 + 조명연산
조사하는 이유 : 버텍스 프로세싱을 지원하는 하드웨어인지 알기위해서 

전면 = 출력만 하는 버퍼
후면버퍼는 무조건 지우고 다음화면에 그릴화면을 준비해준다.
스왑체인을 통해서 깜빡거림을 제어할 수 있다.

래퍼런스카운트 복습 !! 스마트포인터도 래퍼런스카운트를사용하고있고
래퍼런스카운트를 사용하는 포인터도 공부해오기

랜더링 과정 : 후면버퍼를 비운다 -> 후면 버퍼에 그린다 -> 후면 버퍼와 전면 버퍼를 교체 -> 화면 출력



