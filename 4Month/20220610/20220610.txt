10진수를 2진수로 치환해서 4개의 비트라고 가정하면
16가지의 몬스터 스테이트 패턴을 구현할 수 있다.
또한 16진수로 중복 키입력을 구현할 수 있다.

처음 변수를 0x00으로 선언하고
조건에 해당하면 |= or연산을 진행하여 중복적용을 구현할 수 있다.

체크박스 옵션에 푸쉬라이크를 트루로 두면 버튼 모양으로 바뀐다.

버튼에 이미지를 입히려면 속성에 비트맵을 트루로 변경해주고
m_Bit맵이라는 변수를 생성한다.

OnInitDialog에서 비트맵 개방을 실행

툴은 편리하기위해 만드는 것 이다.

정해져있는 곳에 있는경로를 추가하는게 더 편하다.

맵컨테이너는 이터레이터가 아닌 키값으로도 삭제가 가능하다.

창을 생성할 때 모달방식과 모달리스 방식이 있다.

모달방식은 해당창에서만 메세지가 들어올수있고
모달리스 방식은 어디서든 메세지가 들어올수있다.

파일저장에서 || 연산자는 구분자 또는 토큰 이라고 부른다. 
문자열 단위별로 끊어주는 연산자
/ \ | || 같은 기호로 사용한다.

경로의 크기
MAXPATH = 260
유니코드 기반이기때문에 540까지 가능하다.

현재 경로를 얻어오는함수
전체 경로에서 파일 이름만 잘라주는 함수 (만약 경로 상에 파일명이 없다면 제일 마지막 폴더명을 잘라낸다.)
그래서 잘라진 경로에 내가 원하는 폴더명을 합성
lstrcat(szPath, L"\\Data");
Dlg.m_ofn.lpstrInitialDir = szPath; 기본경로로 설정

GetString = 원시 문자열로 반환하는 함수

드랍을 하려면 sz파일을 트루로 바꾼다.

0xffffffff 일 땐 드랍된 갯수를 반환
아닐 땐 경로를 반환

OnDropFiles라는 함수 추가

스크롤을 생성하려면 속성에서 호리젠탈스크롤을 트루로 바꿔줘야한다.

절대경로를 상대로경로로 변경하기
클래스안에 전역함수 생성
상대경로를 저장할변수
기존절대경로를 저장할변수
경로를 불러오는함수
경로를 바꿔주는 함수

파일명만 남기기 = 함수가 있다.

이미지 속성에서 static이아니라 picture로 바꿔야한다.
그리고 변수로 m_Picture 컨트롤타입 변수를 추가한다.

이미지를 찾아서 셑비트맵으로 설정해준다.
속성에 타입이있는데 프레임이 아니라 텍스쳐(bmp)로 바꿔줘야한다

숙제 : 맵툴에서 바꼇을 때 실제 피킹할 때 적용되게 하기.

나가기전에 출석부 꼭 받아라 현수야